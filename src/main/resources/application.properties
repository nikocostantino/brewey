kbe.rest.brewery.jwtSecret = jwtSecretKey
kbe.rest.brewery.jwtExpirationMs = 86400000

spring.datasource.platform=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/fincons-kbe-rest-brewery-jwt
spring.datasource.username=postgres
spring.datasource.password=ENC(m5osdSV7ywMaD+8Nk20Ezw==)
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8081

jasypt.encryptor.algorithm=PBEWithMD5AndDES
jasypt.encryptor.iv-generator-classname=org.jasypt.iv.NoIvGenerator

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/sfgrestbrewery.p12
# The password used to generate the certificate
server.ssl.key-store-password=finacad22
# The alias mapped to the certificate
server.ssl.key-alias=baeldung
server.ssl.enabled=true

spring-doc.api-docs.enabled=true
spring-doc.swagger-ui.enabled=true
spring-doc.api-docs.path=/api-docs

# https://localhost:8081/swagger-ui/index.html#
